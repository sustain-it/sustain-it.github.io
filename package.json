{
  "dependencies": {
    "bootstrap": "^5.1.3"
  },
  "scripts": {
    "build": "npm run sass",
    "sass": "sass --watch --update --style=expanded scss:css",
    "deploy": "npm run build"
  },
  "devDependencies": {
    "sass": "^1.51.0"
  }
}
